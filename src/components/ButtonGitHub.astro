<a href="https://github.com/StreamByte/website" target="_blank" rel="noreferrer" class="github-button">
    <div class="button-content">
        <img src="/assets/icons/star.svg" alt="" class="github-icon">
        <slot />
    </div>
</a>

<style is:global>
    .github-button {
        font-family: var(--font-primary);
        font-size: var(--font-base);
        font-weight: var(--font-bold);
        color: var(--primary-900);
        background-color: var(--shades-white);
        border: 2px solid var(--primary-900);
        border-radius: 8px;
        position: relative;
        padding: 8px 14px;
        text-decoration: none;
        display: inline-block;
        transition: 120ms;
        z-index: 1;
    }

    .button-content {
        display: flex;
        align-items: center;
        position: relative;
    }

    .github-icon {
        width: 20px;
        margin-right: 10px;
    }

    .github-button:hover {
        transform: translate(-4px, -4px);
        box-shadow: 4px 4px 0 var(--primary-900);
        background-color: var(--primary-300);
    }
</style>